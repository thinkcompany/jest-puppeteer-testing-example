image: thinkcompany/docker-node-chrome:latest

pipelines:
  pull-requests:
    feature/*:
      - step:
          script:
            - npm install --quiet
            - npm run test:ci
          caches:
            - node
          artifacts:
            - src/**/__image_snapshots__/__diff_output__/*.png
            - .npm/**/*.log
